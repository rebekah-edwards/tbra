# tbr*a — native iOS (SwiftUI)

**Status (updated 2026-07-01, Mac session):** ✅ **Builds and runs on the iOS 27 simulator** (Xcode 27.0 beta, `iphonesimulator27.0`). The app launches, restores a session from the Keychain, loads Shelves + Up Next from the live `/api/v1`, renders book covers, and reflects a persisted reorder across a cold relaunch. API verified live; Codable models verified byte-for-byte against real responses. The Xcode project is **generated by `xcodegen`** from `project.yml` — no manual "New Project" step. Reorder is plain `List`+`.onMove` (nothing iOS-27-only yet), so it also builds on older SDKs if the deployment target is lowered.

**Fixes made this session** (beyond the two Swift 6 fixes below): the app is now **ad-hoc code-signed** for the simulator with a **keychain entitlement** (`Tbra.entitlements`). This is required — without a signing entitlement the Keychain `SecItemAdd` fails with `errSecMissingEntitlement` (-34018) and auth can't persist. Do **not** set `CODE_SIGNING_ALLOWED=NO`.

**First compile against the real SDK: `BUILD SUCCEEDED` with zero errors** — the pre-applied Swift 6 fixes were exactly what iOS 27's Swift-6-default needed.

## What was verified on the Mac (2026-07-01)
- **API (`/api/v1`) — all green** via the smoke script in `docs/native-api-contract.md`: login → token+refreshToken+user, `/auth/me`, shelves + Up Next reads, shelf **and** Up Next reorder round-trips, incomplete-set reorder → `400`, no/bad token → `401`, `/auth/refresh` → new pair. `auth_refresh_tokens` created on local **and** Turso.
- **Codable models — exact match** to live JSON (field names + nullability) for `ShelfSummary`, `ShelfDetail`, `ShelfBook`, `UpNextItem`, `PublicUser` and all response envelopes. This rules out the decode-mismatch → blank-screen class of bug.
- **Swift 6 fixes applied** (Xcode 27 defaults to the Swift 6 language mode):
  - `@MainActor` on `AuthStore`, `UpNextModel`, `ShelvesModel`, `ShelfDetailModel` (SwiftUI-driven observables that mutate state from `Task`s).
  - `nonisolated(unsafe) static var baseURL` in `APIClient` (a mutable static is otherwise a hard Swift 6 error).
- **Compiled + ran on iOS 27 sim** — `Tab {}` / `AsyncImage` / `ContentUnavailableView` / `@Observable` all resolve cleanly; no residual strict-concurrency diagnostics. Verified: session restore, Shelves + Up Next load with covers, reorder persistence across relaunch.
- **Not exercised** (this Xcode install is missing `Simulator.app`, so there's no GUI for touch input): the physical drag *gesture* and in-app typing. The reorder write path itself is the same `PUT …/order` verified in Step A; only the on-screen drag was untested. Complete the Xcode install (or use a full Xcode) to drive the GUI.

## Generating / building the project
The `.xcodeproj` is **not committed** — it's generated. On the Mac:
```bash
brew install xcodegen          # already installed in this session
cd native-ios && xcodegen generate   # produces Tbra.xcodeproj from project.yml
```
`project.yml` sets: iOS 27.0 deployment target, bundle id `app.tbra.ios`, and an ATS `NSAllowsLocalNetworking` exception so the simulator can reach `http://localhost:3000`. Then build/run with Xcode 27 or `xcodebuild -project Tbra.xcodeproj -scheme Tbra -destination 'platform=iOS Simulator,name=<iOS 27 device>'`.

## What this is
The Shelves + Up Next screens (the first native surfaces) coded against the JSON API in `docs/native-api-contract.md`. It talks to the existing Next.js backend — there is no separate server.

## Requirements
- **Xcode 27** (beta) + an iOS 27 simulator/device — the drag-and-drop APIs are iOS 27-only.
- The tbr*a backend running (dev: `npm run dev` on the Mac, or point `APIClient.baseURL` at production).

## Files
| File | Purpose |
|---|---|
| `Models.swift` | `Codable` structs mirroring the API contract (PublicUser, UpNextItem, ShelfSummary, ShelfDetail, ShelfBook) + response envelopes |
| `Keychain.swift` | Minimal Keychain wrapper — stores the access + refresh tokens (OS-encrypted) |
| `APIClient.swift` | `URLSession` client with bearer auth and **automatic access-token refresh on 401** (rotation) |
| `AuthStore.swift` | `@Observable` session: login / logout / launch session-restore |
| `UpNextView.swift` | The queue, with iOS 27 drag-to-reorder |
| `ShelvesView.swift` | Shelf list |
| `ShelfDetailView.swift` | One shelf's books, with iOS 27 drag-to-reorder |

## First steps on the Mac
1. **Verify the API first** — run the smoke script in `docs/native-api-contract.md` against your dev server. ✅ Done 2026-07-01 (see status above).
2. `cd native-ios && xcodegen generate` — no manual "New Project" needed; `project.yml` already sets min iOS 27, bundle id, ATS, and includes all `*.swift`.
3. `APIClient.baseURL` already defaults to `http://localhost:3000` (reaches a Mac-hosted dev server from the simulator); the ATS `NSAllowsLocalNetworking` exception is baked into `project.yml` → `Info.plist`.
4. Root view is `RootView` (in `AuthStore.swift`), wired via `TbraApp.swift` `@main`.
5. Requires **Xcode 27 beta** on the Mac to compile/run (iOS 27 SDK + simulator).

## Reorder contract reminder
The reorder endpoints require the **complete** current ordering. Each view rebuilds the full ordered ID list from local state after a move and sends that — see the `reorder` calls in the two views.
